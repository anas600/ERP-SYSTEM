// <auto-generated />
// Generated from stock_reservations.json by DEC-092 Repository scaffold generator
// NOTE: SCAFFOLD - manual review and refinement required.

using Dapper;
using ERPSystem.Modules.Inventory.Entities;
using ERPSystem.Shared.Infrastructure;

namespace ERPSystem.Generated.Repositories;

public sealed class StockReservationRepository
{
    private readonly IDbConnectionFactory _db;
    public StockReservationRepository(IDbConnectionFactory db) => _db = db;

    public async Task<StockReservation?> GetByIdAsync(Guid id, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return await conn.QueryFirstOrDefaultAsync<StockReservation?>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE id = @Id LIMIT 1",
            new { Id = id }, cancellationToken: ct));
    }

    public async Task<List<StockReservation>> ListAsync(Guid tenantId, int skip, int take, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return (await conn.QueryAsync<StockReservation>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE tenant_id = @T ORDER BY created_at DESC OFFSET @Skip LIMIT @Take",
            new { T = tenantId, Skip = skip, Take = take }, cancellationToken: ct))).AsList();
    }

    public async Task InsertAsync(StockReservation entity, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        await conn.ExecuteAsync(new CommandDefinition(
            "INSERT INTO " + Table + " (id, tenant_id, item_id, warehouse_id, quantity, reference_type, reference_id, expires_at, created_at, created_by) VALUES (@id, @tenant_id, @item_id, @warehouse_id, @quantity, @reference_type, @reference_id, @expires_at, @created_at, @created_by)",
            entity, cancellationToken: ct));
    }

    private string Table => "stock_reservations";
    private string Columns => "id, tenant_id, item_id, warehouse_id, quantity, reference_type, reference_id, expires_at, created_at, created_by";
    private string Values => "id, @tenant_id, @item_id, @warehouse_id, @quantity, @reference_type, @reference_id, @expires_at, @created_at, @created_by";
}
