// <auto-generated />
// Generated from refresh_tokens.json by DEC-092 Repository scaffold generator
// NOTE: SCAFFOLD - manual review and refinement required.

using Dapper;
using ERPSystem.Shared.Infrastructure;

namespace ERPSystem.Generated.Repositories;

public sealed class RefreshTokenRepository
{
    private readonly IDbConnectionFactory _db;
    public RefreshTokenRepository(IDbConnectionFactory db) => _db = db;

    public async Task<RefreshToken?> GetByIdAsync(Guid id, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return await conn.QueryFirstOrDefaultAsync<RefreshToken?>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE id = @Id LIMIT 1",
            new { Id = id }, cancellationToken: ct));
    }

    public async Task<List<RefreshToken>> ListAsync(Guid tenantId, int skip, int take, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return (await conn.QueryAsync<RefreshToken>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE tenant_id = @T ORDER BY created_at DESC OFFSET @Skip LIMIT @Take",
            new { T = tenantId, Skip = skip, Take = take }, cancellationToken: ct))).AsList();
    }

    public async Task InsertAsync(RefreshToken entity, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        await conn.ExecuteAsync(new CommandDefinition(
            "INSERT INTO " + Table + " (id, user_id, token_hash, expires_at, created_at, revoked_at, replaced_by_token_hash, revoked_reason, created_by_ip, revoked_by_ip) VALUES (@id, @user_id, @token_hash, @expires_at, @created_at, @revoked_at, @replaced_by_token_hash, @revoked_reason, @created_by_ip, @revoked_by_ip)",
            entity, cancellationToken: ct));
    }

    private string Table => "refresh_tokens";
    private string Columns => "id, user_id, token_hash, expires_at, created_at, revoked_at, replaced_by_token_hash, revoked_reason, created_by_ip, revoked_by_ip";
    private string Values => "id, @user_id, @token_hash, @expires_at, @created_at, @revoked_at, @replaced_by_token_hash, @revoked_reason, @created_by_ip, @revoked_by_ip";
}
