// <auto-generated />
// Generated from attendance.json by DEC-092 Repository scaffold generator
// NOTE: SCAFFOLD - manual review and refinement required.

using Dapper;
using ERPSystem.Modules.HR.Entities;
using ERPSystem.Shared.Infrastructure;

namespace ERPSystem.Generated.Repositories;

public sealed class AttendanceRepository
{
    private readonly IDbConnectionFactory _db;
    public AttendanceRepository(IDbConnectionFactory db) => _db = db;

    public async Task<Attendance?> GetByIdAsync(Guid id, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return await conn.QueryFirstOrDefaultAsync<Attendance?>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE id = @Id LIMIT 1",
            new { Id = id }, cancellationToken: ct));
    }

    public async Task<List<Attendance>> ListAsync(Guid tenantId, int skip, int take, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return (await conn.QueryAsync<Attendance>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE tenant_id = @T ORDER BY created_at DESC OFFSET @Skip LIMIT @Take",
            new { T = tenantId, Skip = skip, Take = take }, cancellationToken: ct))).AsList();
    }

    public async Task InsertAsync(Attendance entity, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        await conn.ExecuteAsync(new CommandDefinition(
            "INSERT INTO " + Table + " (id, tenant_id, employee_id, type, timestamp, notes, ip_address, created_at) VALUES (@id, @tenant_id, @employee_id, @type, @timestamp, @notes, @ip_address, @created_at)",
            entity, cancellationToken: ct));
    }

    
    public async Task UpdateAsync(Attendance entity, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        await conn.ExecuteAsync(new CommandDefinition(
            $"UPDATE attendance SET employee_id = @employee_id, type = @type, timestamp = @timestamp, notes = @notes, ip_address = @ip_address WHERE id = @Id AND tenant_id = @TenantId",
            entity, cancellationToken: ct));
    }

    public async Task DeleteAsync(Guid id, Guid tenantId, Guid userId, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        await conn.ExecuteAsync(new CommandDefinition("DELETE FROM attendance WHERE id = @Id AND tenant_id = @TenantId",
            new { Id = id, TenantId = tenantId, UserId = userId }, cancellationToken: ct));
    }

    public async Task<int> CountAsync(Guid tenantId, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return await conn.ExecuteScalarAsync<int>(new CommandDefinition(
            $"SELECT COUNT(*) FROM attendance WHERE tenant_id = @TenantId",
            new { TenantId = tenantId }, cancellationToken: ct));
    }

private string Table => "attendance";
    private string Columns => "id, tenant_id, employee_id, type, timestamp, notes, ip_address, created_at";
    private string Values => "id, @tenant_id, @employee_id, @type, @timestamp, @notes, @ip_address, @created_at";
}
