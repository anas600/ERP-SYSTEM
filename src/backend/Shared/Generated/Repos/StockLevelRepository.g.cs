// <auto-generated />
// Generated from stock_levels.json by DEC-092 Repository scaffold generator
// NOTE: SCAFFOLD - manual review and refinement required.

using Dapper;
using ERPSystem.Modules.Inventory.Entities;
using ERPSystem.Shared.Infrastructure;

namespace ERPSystem.Generated.Repositories;

public sealed class StockLevelRepository
{
    private readonly IDbConnectionFactory _db;
    public StockLevelRepository(IDbConnectionFactory db) => _db = db;

    public async Task<StockLevel?> GetByIdAsync(Guid id, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return await conn.QueryFirstOrDefaultAsync<StockLevel?>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE id = @Id LIMIT 1",
            new { Id = id }, cancellationToken: ct));
    }

    public async Task<List<StockLevel>> ListAsync(Guid tenantId, int skip, int take, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return (await conn.QueryAsync<StockLevel>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE tenant_id = @T ORDER BY created_at DESC OFFSET @Skip LIMIT @Take",
            new { T = tenantId, Skip = skip, Take = take }, cancellationToken: ct))).AsList();
    }

    public async Task InsertAsync(StockLevel entity, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        await conn.ExecuteAsync(new CommandDefinition(
            "INSERT INTO " + Table + " (id, tenant_id, company_id, item_id, warehouse_id, quantity_on_hand, quantity_reserved, average_cost, last_movement_at, version) VALUES (@id, @tenant_id, @company_id, @item_id, @warehouse_id, @quantity_on_hand, @quantity_reserved, @average_cost, @last_movement_at, @version)",
            entity, cancellationToken: ct));
    }

    private string Table => "stock_levels";
    private string Columns => "id, tenant_id, company_id, item_id, warehouse_id, quantity_on_hand, quantity_reserved, average_cost, last_movement_at, version";
    private string Values => "id, @tenant_id, @company_id, @item_id, @warehouse_id, @quantity_on_hand, @quantity_reserved, @average_cost, @last_movement_at, @version";
}
