// <auto-generated />
// Generated from payroll_items.json by DEC-092 Repository scaffold generator
// NOTE: SCAFFOLD - manual review and refinement required.

using Dapper;
using ERPSystem.Modules.Payroll.Domain.Entities;
using ERPSystem.Shared.Infrastructure;

namespace ERPSystem.Generated.Repositories;

public sealed class PayrollItemRepository
{
    private readonly IDbConnectionFactory _db;
    public PayrollItemRepository(IDbConnectionFactory db) => _db = db;

    public async Task<PayrollItem?> GetByIdAsync(Guid id, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return await conn.QueryFirstOrDefaultAsync<PayrollItem?>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE id = @Id LIMIT 1",
            new { Id = id }, cancellationToken: ct));
    }

    public async Task<List<PayrollItem>> ListAsync(Guid tenantId, int skip, int take, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return (await conn.QueryAsync<PayrollItem>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE tenant_id = @T ORDER BY created_at DESC OFFSET @Skip LIMIT @Take",
            new { T = tenantId, Skip = skip, Take = take }, cancellationToken: ct))).AsList();
    }

    public async Task InsertAsync(PayrollItem entity, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        await conn.ExecuteAsync(new CommandDefinition(
            "INSERT INTO " + Table + " (id, tenant_id, payroll_run_id, employee_id, base_salary, gross_salary, tax_amount, social_insurance_employee, net_salary, status, payment_days, notes, created_at, created_by, updated_at) VALUES (@id, @tenant_id, @payroll_run_id, @employee_id, @base_salary, @gross_salary, @tax_amount, @social_insurance_employee, @net_salary, @status, @payment_days, @notes, @created_at, @created_by, @updated_at)",
            entity, cancellationToken: ct));
    }

    private string Table => "payroll_items";
    private string Columns => "id, tenant_id, payroll_run_id, employee_id, base_salary, gross_salary, tax_amount, social_insurance_employee, net_salary, status, payment_days, notes, created_at, created_by, updated_at";
    private string Values => "id, @tenant_id, @payroll_run_id, @employee_id, @base_salary, @gross_salary, @tax_amount, @social_insurance_employee, @net_salary, @status, @payment_days, @notes, @created_at, @created_by, @updated_at";
}
