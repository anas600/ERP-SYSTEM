// <auto-generated />
// Generated from item_categories.json by DEC-092 Repository scaffold generator
// NOTE: SCAFFOLD - manual review and refinement required.

using Dapper;
using ERPSystem.Modules.Inventory.Entities;
using ERPSystem.Shared.Infrastructure;

namespace ERPSystem.Generated.Repositories;

public sealed class ItemCategoryRepository
{
    private readonly IDbConnectionFactory _db;
    public ItemCategoryRepository(IDbConnectionFactory db) => _db = db;

    public async Task<ItemCategory?> GetByIdAsync(Guid id, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return await conn.QueryFirstOrDefaultAsync<ItemCategory?>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE id = @Id LIMIT 1",
            new { Id = id }, cancellationToken: ct));
    }

    public async Task<List<ItemCategory>> ListAsync(Guid tenantId, int skip, int take, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return (await conn.QueryAsync<ItemCategory>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE tenant_id = @T ORDER BY created_at DESC OFFSET @Skip LIMIT @Take",
            new { T = tenantId, Skip = skip, Take = take }, cancellationToken: ct))).AsList();
    }

    public async Task InsertAsync(ItemCategory entity, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        await conn.ExecuteAsync(new CommandDefinition(
            "INSERT INTO " + Table + " (id, tenant_id, code, name, description, parent_id, is_active, created_at, updated_at) VALUES (@id, @tenant_id, @code, @name, @description, @parent_id, @is_active, @created_at, @updated_at)",
            entity, cancellationToken: ct));
    }

    
    public async Task UpdateAsync(ItemCategory entity, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        entity.UpdatedAt = DateTime.UtcNow;
        await conn.ExecuteAsync(new CommandDefinition(
            $"UPDATE item_categories SET code = @code, name = @name, description = @description, parent_id = @parent_id, is_active = @is_active, updated_at = @updated_at WHERE id = @Id AND tenant_id = @TenantId",
            entity, cancellationToken: ct));
    }

    public async Task DeleteAsync(Guid id, Guid tenantId, Guid userId, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        await conn.ExecuteAsync(new CommandDefinition("DELETE FROM item_categories WHERE id = @Id AND tenant_id = @TenantId",
            new { Id = id, TenantId = tenantId, UserId = userId }, cancellationToken: ct));
    }

    public async Task<int> CountAsync(Guid tenantId, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return await conn.ExecuteScalarAsync<int>(new CommandDefinition(
            $"SELECT COUNT(*) FROM item_categories WHERE tenant_id = @TenantId",
            new { TenantId = tenantId }, cancellationToken: ct));
    }

private string Table => "item_categories";
    private string Columns => "id, tenant_id, code, name, description, parent_id, is_active, created_at, updated_at";
    private string Values => "id, @tenant_id, @code, @name, @description, @parent_id, @is_active, @created_at, @updated_at";
}
