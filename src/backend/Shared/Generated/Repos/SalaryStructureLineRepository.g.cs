// <auto-generated />
// Generated from salary_structure_lines.json by DEC-092 Repository scaffold generator
// NOTE: SCAFFOLD - manual review and refinement required.

using Dapper;
using ERPSystem.Modules.Payroll.Domain.Entities;
using ERPSystem.Shared.Infrastructure;

namespace ERPSystem.Generated.Repositories;

public sealed class SalaryStructureLineRepository
{
    private readonly IDbConnectionFactory _db;
    public SalaryStructureLineRepository(IDbConnectionFactory db) => _db = db;

    public async Task<SalaryStructureLine?> GetByIdAsync(Guid id, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return await conn.QueryFirstOrDefaultAsync<SalaryStructureLine?>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE id = @Id LIMIT 1",
            new { Id = id }, cancellationToken: ct));
    }

    public async Task<List<SalaryStructureLine>> ListAsync(Guid tenantId, int skip, int take, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return (await conn.QueryAsync<SalaryStructureLine>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE tenant_id = @T ORDER BY created_at DESC OFFSET @Skip LIMIT @Take",
            new { T = tenantId, Skip = skip, Take = take }, cancellationToken: ct))).AsList();
    }

    public async Task InsertAsync(SalaryStructureLine entity, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        await conn.ExecuteAsync(new CommandDefinition(
            "INSERT INTO " + Table + " (id, tenant_id, salary_structure_id, type, name, formula, amount, sort_order) VALUES (@id, @tenant_id, @salary_structure_id, @type, @name, @formula, @amount, @sort_order)",
            entity, cancellationToken: ct));
    }

    private string Table => "salary_structure_lines";
    private string Columns => "id, tenant_id, salary_structure_id, type, name, formula, amount, sort_order";
    private string Values => "id, @tenant_id, @salary_structure_id, @type, @name, @formula, @amount, @sort_order";
}
