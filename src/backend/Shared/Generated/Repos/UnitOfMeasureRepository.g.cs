// <auto-generated />
// Generated from units_of_measure.json by DEC-092 Repository scaffold generator
// NOTE: SCAFFOLD - manual review and refinement required.

using Dapper;
using ERPSystem.Shared.Infrastructure;

namespace ERPSystem.Generated.Repositories;

public sealed class UnitOfMeasureRepository
{
    private readonly IDbConnectionFactory _db;
    public UnitOfMeasureRepository(IDbConnectionFactory db) => _db = db;

    public async Task<UnitOfMeasure?> GetByIdAsync(Guid id, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return await conn.QueryFirstOrDefaultAsync<UnitOfMeasure?>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE id = @Id LIMIT 1",
            new { Id = id }, cancellationToken: ct));
    }

    public async Task<List<UnitOfMeasure>> ListAsync(Guid tenantId, int skip, int take, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return (await conn.QueryAsync<UnitOfMeasure>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE tenant_id = @T ORDER BY created_at DESC OFFSET @Skip LIMIT @Take",
            new { T = tenantId, Skip = skip, Take = take }, cancellationToken: ct))).AsList();
    }

    public async Task InsertAsync(UnitOfMeasure entity, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        await conn.ExecuteAsync(new CommandDefinition(
            "INSERT INTO " + Table + " (id, tenant_id, code, name, symbol, is_active, created_at) VALUES (@id, @tenant_id, @code, @name, @symbol, @is_active, @created_at)",
            entity, cancellationToken: ct));
    }

    private string Table => "units_of_measure";
    private string Columns => "id, tenant_id, code, name, symbol, is_active, created_at";
    private string Values => "id, @tenant_id, @code, @name, @symbol, @is_active, @created_at";
}
