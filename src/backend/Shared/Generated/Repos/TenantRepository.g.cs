// <auto-generated />
// Generated from tenants.json by DEC-092 Repository scaffold generator
// NOTE: SCAFFOLD - manual review and refinement required.

using Dapper;
using ERPSystem.Modules.Identity.Entities;
using ERPSystem.Shared.Infrastructure;

namespace ERPSystem.Generated.Repositories;

public sealed class TenantRepository
{
    private readonly IDbConnectionFactory _db;
    public TenantRepository(IDbConnectionFactory db) => _db = db;

    public async Task<Tenant?> GetByIdAsync(Guid id, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return await conn.QueryFirstOrDefaultAsync<Tenant?>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE id = @Id LIMIT 1",
            new { Id = id }, cancellationToken: ct));
    }

    public async Task<List<Tenant>> ListAsync(Guid tenantId, int skip, int take, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return (await conn.QueryAsync<Tenant>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE tenant_id = @T ORDER BY created_at DESC OFFSET @Skip LIMIT @Take",
            new { T = tenantId, Skip = skip, Take = take }, cancellationToken: ct))).AsList();
    }

    public async Task InsertAsync(Tenant entity, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        await conn.ExecuteAsync(new CommandDefinition(
            "INSERT INTO " + Table + " (id, name, subdomain, is_active, created_at, subscription_expires_at) VALUES (@id, @name, @subdomain, @is_active, @created_at, @subscription_expires_at)",
            entity, cancellationToken: ct));
    }

    private string Table => "tenants";
    private string Columns => "id, name, subdomain, is_active, created_at, subscription_expires_at";
    private string Values => "id, @name, @subdomain, @is_active, @created_at, @subscription_expires_at";
}
