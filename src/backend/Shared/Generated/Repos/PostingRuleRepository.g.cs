// <auto-generated />
// Generated from posting_rules.json by DEC-092 Repository scaffold generator
// NOTE: SCAFFOLD - manual review and refinement required.

using Dapper;
using ERPSystem.Modules.Finance.Entities;
using ERPSystem.Shared.Infrastructure;

namespace ERPSystem.Generated.Repositories;

public sealed class PostingRuleRepository
{
    private readonly IDbConnectionFactory _db;
    public PostingRuleRepository(IDbConnectionFactory db) => _db = db;

    public async Task<PostingRule?> GetByIdAsync(Guid id, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return await conn.QueryFirstOrDefaultAsync<PostingRule?>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE id = @Id LIMIT 1",
            new { Id = id }, cancellationToken: ct));
    }

    public async Task<List<PostingRule>> ListAsync(Guid tenantId, int skip, int take, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return (await conn.QueryAsync<PostingRule>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE tenant_id = @T ORDER BY created_at DESC OFFSET @Skip LIMIT @Take",
            new { T = tenantId, Skip = skip, Take = take }, cancellationToken: ct))).AsList();
    }

    public async Task InsertAsync(PostingRule entity, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        await conn.ExecuteAsync(new CommandDefinition(
            "INSERT INTO " + Table + " (id, tenant_id, name, description, event_type, is_active, template_json, created_at, updated_at) VALUES (@id, @tenant_id, @name, @description, @event_type, @is_active, @template_json, @created_at, @updated_at)",
            entity, cancellationToken: ct));
    }

    private string Table => "posting_rules";
    private string Columns => "id, tenant_id, name, description, event_type, is_active, template_json, created_at, updated_at";
    private string Values => "id, @tenant_id, @name, @description, @event_type, @is_active, @template_json, @created_at, @updated_at";
}
