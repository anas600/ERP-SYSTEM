// <auto-generated />
// Generated from leave_requests.json by DEC-092 Repository scaffold generator
// NOTE: SCAFFOLD - manual review and refinement required.

using Dapper;
using ERPSystem.Shared.Infrastructure;

namespace ERPSystem.Generated.Repositories;

public sealed class LeaveRequestRepository
{
    private readonly IDbConnectionFactory _db;
    public LeaveRequestRepository(IDbConnectionFactory db) => _db = db;

    public async Task<LeaveRequest?> GetByIdAsync(Guid id, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return await conn.QueryFirstOrDefaultAsync<LeaveRequest?>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE id = @Id LIMIT 1",
            new { Id = id }, cancellationToken: ct));
    }

    public async Task<List<LeaveRequest>> ListAsync(Guid tenantId, int skip, int take, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        return (await conn.QueryAsync<LeaveRequest>(new CommandDefinition(
            "SELECT * FROM " + Table + " WHERE tenant_id = @T ORDER BY created_at DESC OFFSET @Skip LIMIT @Take",
            new { T = tenantId, Skip = skip, Take = take }, cancellationToken: ct))).AsList();
    }

    public async Task InsertAsync(LeaveRequest entity, CancellationToken ct)
    {
        using var conn = await _db.CreateOltpConnectionAsync(ct);
        await conn.ExecuteAsync(new CommandDefinition(
            "INSERT INTO " + Table + " (id, tenant_id, employee_id, leave_type, start_date, end_date, total_days, status, reason, approver_id, approved_at, notes, created_at, created_by, updated_at) VALUES (@id, @tenant_id, @employee_id, @leave_type, @start_date, @end_date, @total_days, @status, @reason, @approver_id, @approved_at, @notes, @created_at, @created_by, @updated_at)",
            entity, cancellationToken: ct));
    }

    private string Table => "leave_requests";
    private string Columns => "id, tenant_id, employee_id, leave_type, start_date, end_date, total_days, status, reason, approver_id, approved_at, notes, created_at, created_by, updated_at";
    private string Values => "id, @tenant_id, @employee_id, @leave_type, @start_date, @end_date, @total_days, @status, @reason, @approver_id, @approved_at, @notes, @created_at, @created_by, @updated_at";
}
