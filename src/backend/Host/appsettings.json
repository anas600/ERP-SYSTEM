{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "Microsoft.Hosting.Lifetime": "Information",
        "Microsoft.AspNetCore": "Warning"
      }
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Postgres": "Host=localhost;Database=neondb;Username=erp_user;Password=erp_password;Maximum Pool Size=100;Minimum Pool Size=10;Connection Idle Lifetime=300;Timeout=15;Command Timeout=30  /* Local dev only - HF Space overrides via DB_CONNECTION env var. Pool tuning DEC-109 */",
    "Redis": "localhost:6379"
  },
  "Marten": {
    "ConnectionString": "Host=localhost;Database=erp_events;Username=erp_user;Password=erp_password  /* Local dev only - HF Space overrides via EVENTS_CONNECTION env var (see DEC-019, DEC-021) */",
    "SchemaName": "mt_events",
    "_comment": "Configured but DISABLED (DEC-017). Enable in Sprint-5+ for event sourcing."
  },
  "JwtSettings": {
    "Secret": "REPLACE_WITH_SECURE_SECRET_AT_LEAST_32_CHARS_LONG_DEV_ONLY",
    "Issuer": "ERP-SYSTEM",
    "Audience": "ERP-SYSTEM-Users",
    "AccessTokenExpiryMinutes": 60,
    "RefreshTokenExpiryDays": 14
  },
  "Database": {
    "AutoMigrate": true,
    "SeedScenario": true,
    "SeedAlFajrScenario": false,
    "SeedAlBurjScenario": false,
    "SeedRealisticScenario": true,
    "JsonMigrationEnabled": true,
    "_seed_comment": "SPRINT-4 (DEC-023, DEC-032) + Sprint-4.5 (DEC-064/067/068). After DEC-067 (BackgroundService fix), RealisticSeed no longer blocks startup. Safe to re-enable. AlFajr disabled to avoid duplicates. SeedAlBurjScenario MUST be false (DEC-009). SeedScenario is kept for backward compatibility with ScenarioSeederHostedService. JsonMigrationEnabled added DEC-079 (PoC for JSON-driven additive schema changes)."
  }
}
