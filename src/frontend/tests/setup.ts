// Test setup — runs before each test file.
//
// Loads @testing-library/jest-dom which adds the
//   - toBeInTheDocument()
//   - toHaveClass()
//   - toHaveAttribute()
//   - ...
// matchers to the global `expect()`.

import '@testing-library/jest-dom';
