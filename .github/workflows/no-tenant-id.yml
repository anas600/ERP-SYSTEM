name: Architecture Guard — no tenant_id (DISABLED 2026-08-27)

# This workflow was disabled per Anas's directive 2026-08-27 13:50 UTC+2.
# The check has been removed. See CHANGELOG and AGENTS.md for rationale.

on:
  pull_request:
    branches: [develop, main]

jobs:
  no-tenant-id:
    name: No new tenant_id in PR (DISABLED)
    runs-on: ubuntu-latest
    steps:
      - name: This check is disabled
        run: |
          echo "Architecture guard is DISABLED per owner directive 2026-08-27."
          echo "Constitution Article 3 compliance is enforced by code review and tests."
          exit 0
